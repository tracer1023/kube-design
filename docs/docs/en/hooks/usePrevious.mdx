---
group: 'hooks'
category: 'state'
title: usePrevious
description: '用来存储 props 或以前的状态'
order: 1
---

## 使用方法

`usePrevious` hook 可以用来存储 props 或以前的状态。

```js live=true
() => {
  const [value, setValue] = React.useState(0);

  const lastValue = usePrevious(value);
  return (
    <div>
      <p>Current: {value} - previous:{lastValue}</p>
      <Button onClick={()=>setValue(value+1)}>Increment number</Button>
    </div>
  )
}
```

## API

usePrevious hook 接受一个任意类型 value 作为参数。

## 定义

```ts
usePrevious<T>(value: T)
```


